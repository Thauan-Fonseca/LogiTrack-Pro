spring.application.name=api

spring.datasource.url=jdbc:postgresql://localhost:5433/logitrack_pro
spring.datasource.username=logitrack
spring.datasource.password=logitrack123

spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

app.cors.allowed-origins=http://localhost:5173

# Chave usada para assinar os JWTs. Em produção, sobrescrever via variável de
# ambiente JWT_SECRET (nunca versionar o valor real de produção).
app.jwt.secret=${JWT_SECRET:FhyITBDY+c9g7h4ekIVFL/1MEptOJVhPXt1BcFZWPy7jKnG1zugGctzeBTSmhSvW}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:28800000}
